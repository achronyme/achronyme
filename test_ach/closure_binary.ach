fn make_adder(x) {
  // 'x' is captured by the returned closure.
  // The VM needs 'upvalue_info' to know that index 0 is 'x' from parent stack.
  return fn(y) { return x + y; };
}

mut add10 = make_adder(10);
mut result = add10(5);

print(result); // Should print 15
