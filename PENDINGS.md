# Features to Implement

- [ ] Support escaped characters in strings (e.g. `\"`, `\n`) in `grammar.pest`. Current parser stops at first quote.
- [ ] Add `Map` support to `VM::val_to_string` once maps are fully implemented.