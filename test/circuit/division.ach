// Circuit: field division
circuit division(q: Public, a: Witness, b: Witness) {
    // a / b = q  →  a = q * b
    assert_eq(a / b, q)

    // Division by constant
    let half = a / 2
    assert_eq(half * 2, a)
}
