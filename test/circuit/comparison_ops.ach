// Circuit: comparison operators
circuit comparison_ops(x: Witness, y: Witness) {
    // x=3, y=5 expected
    assert(x < y)
    assert(x <= y)
    assert(x != y)
    assert(!(x == y))

    // Derived comparisons
    assert(y > x)
    assert(y >= x)
}
