// Circuit: nullifier derivation — poseidon(secret, leaf_index)
circuit nullifier(nullifier: Public, secret: Witness, leaf_index: Witness) {
    let nf = poseidon(secret, leaf_index)
    assert_eq(nf, nullifier)
}
