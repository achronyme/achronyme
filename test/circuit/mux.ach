// Circuit: mux (conditional selection)
circuit mux(out: Public, cond: Witness, a: Witness, b: Witness) {
    // mux(cond, a, b) selects a when cond=1, b when cond=0
    let selected = mux(cond, a, b)
    assert_eq(selected, out)
}
